#Circle-CI

#machine:
#  php:
#    version: 7.0.14

checkout:
 post:
   - git clone https://github.com/NycuRO/CI-Test

dependencies:
 override:
  - mkdir plugins
  - cd 
  - wget -O PHP7.zip https://github.com/LeverylTeam/PHP7-Binaries/raw/master/PHP-7.0.14_Linux-x84_64.zip
  - unzip PHP7.zip
  - wget https://raw.githubusercontent.com/PrismarineMC/Prismarine/master/start.sh
  - wget -O plugins/DevTools.phar https://github.com/pmmp/PocketMine-DevTools/releases/download/v1.12.1/DevTools_v1.12.1.phar
#  - pecl install channel://pecl.php.net/pthreads-3.1.6
#  - pecl install channel://pecl.php.net/weakref-0.3.2
#  - echo | pecl install channel://pecl.php.net/yaml-2.0.0RC8

test:
 override:
  - ./bin/php7/bin/php build.php
#  - ls -R
  - cp -rv ./plugins/DevTools/ $CIRCLE_ARTIFACTS
